# Standalone body implementation state

## Current checkpoint: additive typography implementation, 2026-09-04

Current bounded authority supersedes the initial implementation file list below: implement the four proven diagnostic reflow defects and intentional appendix numbering/marks inside this `standalone/` directory plus scoped evidence/tests. Preserve all 828 staged baseline bytes. The parent owns driver generation, bibliography and all TeX/build/publication operations. No TeX process or PDF was created, edited or controlled by this typography subtask.

Completed: six exact inline-to-display rules (two proof-height equations in OLP-0668; quantified equivalence in OLP-0290; encoded formula in OLP-0291; two substitution equations in OLP-0369). Original baseline subfiles remain actual inputs. Original captured formula tokens are emitted unchanged; following sentence punctuation is preserved without isolated punctuation lines. Catcode activation is stable-source-specific, and non-target nested sources explicitly reset to ordinary math shifts. The owner's paragraph-capable hooks, robust command copies, import-path bookkeeping and corrected link arrows/spaces remain intact.

Completed: the alternative-source appendix now has its own counter, visible A.1-A.4 section numbers, unique hyperlink prefix and explicit running marks; appendix pages flush before local definitions restore. Canonical chapter/section counters are not repurposed.

Checks completed: all 828 staged files rehashed against `STAGED_FILE_MANIFEST.json` SHA-256 `97864216624a65e137332d70f6563515c3a17b304362e4267d2d589d4ded4103`; six exact formula payloads and expected single occurrences matched; reversible prose/punctuation model passed; formula-change, duplicate, wrong-source and punctuation negative tests passed. Existing ledger `--check --self-test` also passed with 722 rows and 774 contexts.

Current implementation evidence: `evidence/STANDALONE_TYPOGRAPHY_QA.json`, SHA-256 `d8f6f53d77fe91c9d6781a745f873e260202ff5f9aa20ae43f0eef1235006164`. Its status is static preservation PASS, **not TeX or visual PASS**. Current code hashes are recorded in that receipt; the original implementation hashes below remain historical.

Next executable owner action: run the managed diagnostic/full build, then execute `standalone/tests/check_typography.py --trace-file <new-jobname>.oltypography --trace-log <new-jobname>.log`; require six exact typography events and `COVERAGE|6|6` in addition to existing source/context checks. The sidecar parser has positive and four negative trace tests. Locate these stable source units in new pagination and visually inspect their complete pages plus the appendix. The formerly severe diagnostic pages 695, 767, 221 and 396 remain unreviewed/unchanged by this bounded implementation. Final reference/bibliography/layout/reproducibility QA remains with the parent. Bounded implementation is handed off; no further source edits while an owner build is live.

## Historical initial body/ledger implementation

Authority: bounded body/ledger implementation only. Write scope is this `standalone/` directory, `build/make_standalone_ledger.py`, and `evidence/STANDALONE_INTEGRATION_LEDGER.json`. No root preamble, baseline source, original production tree, PDF, Git, external service, or publication is modified. No TeX is permitted in this subtask.

Primary authority: staged `evidence/controls/CLOSURE_MANIFEST.csv`, SHA-256 `ea3fa09516af7dd55851026ad597820544fc8750aa628f9d28780be02377b9a5`; all 722 staged Persian target bytes must match its `fa_IR_sha256` fields before generation.

Implemented: additive original-graph body dispatch; coherent proof-theory and retained-subject insertions; four isolated alternative drivers; label remapping; deferred-exercise reference context; exact source/context trace and runtime coverage gate; optional owner-controlled errata registration slot.

Material correction from architecture inspection: 52 baseline paths intentionally have distinct propositional/first-order logical contexts. The source ledger has 722 rows, while correct occurrence coverage has 774 context-specific entries. A filename-only deduplication guard would silently remove content; the implemented guard keys source ID plus FOL context.

Static generation passed with 722 unique units, 774 occurrences, 59 proof-theory units, 36 suppressed alternative-wrapper import edges and 40 namespace mappings. All 16 historical reference-alias targets occur in the source label inventory. Deterministic `--check`, positive trace replay, and four negative trace mutations passed. Authored/generated TeX braces were statically balanced. TeX remains unrun.

Terminal condition: bounded body/ledger implementation handed off. `--check --self-test` independently replays all 722 baseline hashes and exact generated bytes. Next owner action is a machine-mutex-guarded diagnostic build, followed by `make_standalone_ledger.py --trace <jobname>.olunits` and the full deterministic QA workflow. This handoff is not a compiled-PDF claim.

Generated artifact SHA-256 identities:

- `generated-ledger.tex`: `ca05c3dedf9ec23a32be75c6556e7cdd94fcbe326b5bb3ec3023cdcb5ab1140e`
- `evidence/STANDALONE_INTEGRATION_LEDGER.json`: `0a6cc8b2799117a3216e25ebe84ecd3c0cd3a665ddd9d55d7e7bad2bdda21dea`
- `build/make_standalone_ledger.py`: `e62ef89de245ff6671e548e6492f429658a3716caf2d314c24cdb9e5aa4746a5`

Import implementation was checked against installed subfiles 2.2/import 6.2 and the baseline `olimport`: the body preserves `ol@sectioncs`, `olfilename`, `olfilebase`, source grouping and FOL tag context. Root-relative baseline subfiles save/reset/restore the globally managed `import@path`. No baseline content directly references `olfilebase`, but its original bookkeeping is preserved anyway.

Remaining deterministic validation: TeX expansion and hook behavior, all actual-context trace entries, namespace/alias AUX convergence, deferred exercise references, floats/RTL/Unicode/layout, links, formula/citation checks and reproducibility. These are operational QA tasks, not human-review gates. The owner-controlled `standalone-errata.tex` registration slot is present and does not alter baseline files.
