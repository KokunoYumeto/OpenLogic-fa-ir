% فرادادهٔ ساخت برای واحد مستقل خواندنی OLP-0008 به فارسی ایران.
% برچسب بخش پذیرفته‌شدهٔ پیشین برای حل ارجاع در ساخت مستقل وارد می‌شود.
\usepackage{xr}
\externaldocument{../../../output/build/olp0005/open-logic-olp0005-fa-IR}
\hypersetup{
  unicode=true,
  pdftitle={اجتماع‌ها و اشتراک‌ها},
  pdfauthor={پروژهٔ منطق باز}
}
