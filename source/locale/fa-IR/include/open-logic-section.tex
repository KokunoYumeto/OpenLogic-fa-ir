\documentclass[article]{memoir}

\usepackage{subfiles}
\usepackage{fontspec}
\usepackage[
  english,
  bidi=basic,
  layout=sectioning counters lists contents footnotes captions tabular
]{babel}
\babelprovide[import=fa,main,onchar=ids fonts]{persian}

\babelfont{rm}[Ligatures=TeX]{TeX Gyre Pagella}
\babelfont{sf}[Ligatures=TeX]{TeX Gyre Heros}
\babelfont{tt}{Latin Modern Mono}
\babelfont[persian]{rm}[
  Path=../../../../../../00_control/fonts/scheherazade-2.100/,
  UprightFont=Scheherazade-Regular.ttf,
  BoldFont=Scheherazade-Bold.ttf,
  AutoFakeSlant=0.15,
  Renderer=HarfBuzz,
  Scale=1.08
]{Scheherazade-Regular.ttf}
\babelfont[persian]{sf}[
  Path=../../../../../../00_control/fonts/scheherazade-2.100/,
  UprightFont=Scheherazade-Regular.ttf,
  BoldFont=Scheherazade-Bold.ttf,
  AutoFakeSlant=0.15,
  Renderer=HarfBuzz,
  Scale=1.08
]{Scheherazade-Regular.ttf}

% A localized section lives five directory levels below this worktree root.
\newcommand*{\olpath}{../../../../..}
\newcommand*{\ollangid}{fa-IR}

% Do not pass "persian" to cleveref: the installed package has no such
% option. Load the locale explicitly while retaining the fa-IR source root.
\input{\olpath/sty/open-logic.sty}
\input{\olpath/locale/\ollangid/open-logic-locale.sty}

\AtBeginDocument{\selectlanguage{persian}}
\hypersetup{pdflang={fa-IR}}

\def\thechapter{\theolchapter}
% Stable Open Logic identifiers are Latin-script tokens.  Isolate each
% complete counter string so Persian bidi reordering cannot display `set.1`
% as `1.set` in section and theorem-like headings.
\renewcommand*{\thesection}{\babelsublr{\theolchapter.\arabic{section}}}
\renewcommand*{\thethm}{\babelsublr{\theolchapter.\arabic{thm}}}
\renewcommand*{\theprob}{\babelsublr{\theolchapter.\arabic{prob}}}
% This is a reader/QA wrapper. Upstream's standalone debug package adds
% margin IDs and is incompatible with this bidi page-number representation.
\pagestyle{plain}

\begin{document}
This localized master is used only when compiling a Persian section subfile.
\end{document}
