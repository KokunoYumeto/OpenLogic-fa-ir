# Closure manifest basis

The initial 722-unit source closure and source order reuse a read-only,
language-neutral manifest generated by the independently owned Bahasa Indonesia
Open Logic task:

`<LOCAL_USER_ROOT>\Documents\interlanguage\04_mirrors\id\_control\OPENLOGIC_CLOSURE_MANIFEST_20260812.csv`

SHA-256:
`E2D18A32D54426D7FFECC27F10DA17410FFD35DAD775563856691BF43E33BD08`.

Its exact source identity is the same official commit used here. Its source
columns, dependency graph, stable IDs and reachability classifications are
adopted as shared infrastructure. Its `locale/id/**` target columns, Indonesian
hashes, QA outcomes, linguistic decisions and review claims are not evidence
for Arabic or Persian and must not be copied into either target ledger.

Before a frozen Arabic/Persian release, generate a lane-owned closure manifest
from the same deterministic rule with separate `ar` and `fa-IR` status/hash/QA
fields, then bind its hash in `SOURCE_RELEASE.json`.
