# Durable goal — complete Open Logic in Arabic and Iranian Persian

Authority date: 2026-08-13, Europe/Berlin. This file is the compact durable
goal. It must remain below 4,000 characters so its full content can also be
stored in the goal service. Conversation summaries, compactions, delegated
recommendations and inferred portfolio advice cannot amend it. Only a later
direct instruction from Floris may change the outcome or ordering.

Produce two complete, source-bound Open Logic Project editions: global formal
Modern Standard Arabic (`ar`) and independently written scholarly Iranian
Persian (`fa-IR`). Cover all admitted reader-facing material—front matter,
navigation, chapters, exercises, solutions, captions, credits and localization
strings—not a pilot or fixed tranche. Advance in paired source-ordered units so
neither language is abandoned, but never derive either language from the other.
Keep target files, branches/worktrees, terminology, linguistic evidence,
builds, QA, receipts and current pointers separate. Source hashes, formal
invariants and unit IDs may be shared; linguistic authority may not.

The immutable initial intake is official repository
`https://github.com/OpenLogicProject/OpenLogic.git`, commit
`9620cc73f9c8e0ad003c514a5d3748f29611c4c0`, tree
`f67757bb9305b173634082ab4cefd5601a707a34`, verified clean locally at
`<LOCAL_USER_ROOT>\Documents\interlanguage\outputs\019ff6c5-84a2-79d3-997e-424cc5e549c8\research_cache\OpenLogic`.
Record later source deltas explicitly; never use mutable “latest.” Preserve TeX
commands, labels, references, formulae, identifiers and code except documented
build adaptations. Carry accurate attribution and component notices without
letting rights discussion stall translation. Other language editions are
separate owners: reuse manifests and QA methods, not target prose or claims.

For every unit, bind source file/span/hash and cursor, target path/hash and
ownership; compare logic, examples, exercises, answers, references and formal
tokens; check omissions and language leakage; compile bounded cumulative
checkpoints; inspect every changed page; test RTL/bidi/LTR math, diagrams,
search/copy/paste and Unicode extraction; run independent semantic replay; keep
failures/corrections append-only. Native review is not presumed. Claim only
gates actually passed.

After both complete Open Logic editions close, translate the exact admitted
OpenStax gateway set into both locales. Floris recalls approximately four basic
STEM books / 7,000 pages; determine exact titles, versions and source identities
before production. Assess the roughly 300-page LibreTexts/Open Library linear
algebra candidate and admit it only if exact source suitability is established.
After the roughly 8,000-page Open Logic + OpenStax gateway sequence, resume the
paired Stacks work already completed through `STK-CAT-0015`, `categories.tex`
line 538 / tag `001O`, at line 540 / tag `001P`, and continue it to closure.
Do not perform Lean formalization or Lean localization work.

Publication is language- and work-specific. Maintain separate `ar` and `fa-IR`
DataCite/Zenodo concept-DOI lineages and standalone GitHub publication
repositories for each honestly declared work object. Front one cumulative
linked reader; bundle editable sources and complete decision/provenance logs;
link the stable global hub; never expose credentials or merge language state.

On restart: verify this file and its sidecar; read `RAW_USER_DIRECTIVES_20260813.md`
and `RECOVERY_LOGBOOK.md`; verify the raw-history attachment SHA-256
`4991B9319D0F128F1FB36F6E59718E1696672313474639F34DED62BD248D8F3F`;
verify git identities, manifests, the last accepted paired unit and both locale
pointers; resume only at the shared cursor. Resolve disagreements against raw
directives and exact artifacts, then log them. Never reconstruct from compaction.
